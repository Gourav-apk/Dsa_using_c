#include<stdio.h>
   int count=0;
int nrecur(int n)
{
  
    count++;
	if(n==0)
	{
		return 1;
	}

	 return n*nrecur(n-1);
}
int main()
{
	int n=5;
	int fact=nrecur(n);
	printf("factorial %d is %d\n",n,fact);
	printf("Function calling is :%d\n"count);
	return 0;
}

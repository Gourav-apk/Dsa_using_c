#include<stdio.h>
using namespace std
int fact(int n)
{
    if(n==0)
    {
        return 0;
    }
    return n*fact(n-1);
}
int main()
{
    int n;
    printf("Enter Entiger no. \n");
    scanf("%d",&n);
    int store=fact(n);
    printf("Factorial is %d",store();)
    return 0;
}
